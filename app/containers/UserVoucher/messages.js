/*
 * UserVoucher Messages
 *
 * This contains all the text for the UserVoucher container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.UserVoucher';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UserVoucher container!',
  },
});
