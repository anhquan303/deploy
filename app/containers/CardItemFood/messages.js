/*
 * CardItemFood Messages
 *
 * This contains all the text for the CardItemFood container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CardItemFood';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CardItemFood container!',
  },
});
