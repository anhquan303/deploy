/*
 * UserReport Messages
 *
 * This contains all the text for the UserReport container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.UserReport';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UserReport container!',
  },
});
