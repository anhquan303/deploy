// import { selectDetailReportDomain } from '../selectors';

describe('selectDetailReportDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
