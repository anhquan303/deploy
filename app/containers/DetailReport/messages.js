/*
 * DetailReport Messages
 *
 * This contains all the text for the DetailReport container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DetailReport';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DetailReport container!',
  },
});
