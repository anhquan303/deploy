/*
 * StoreProfile Messages
 *
 * This contains all the text for the StoreProfile container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.StoreProfile';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the StoreProfile container!',
  },
});
