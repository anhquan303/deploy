// import { selectStoreProfileDomain } from '../selectors';

describe('selectStoreProfileDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
