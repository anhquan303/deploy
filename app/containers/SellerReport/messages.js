/*
 * SellerReport Messages
 *
 * This contains all the text for the SellerReport container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SellerReport';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SellerReport container!',
  },
});
