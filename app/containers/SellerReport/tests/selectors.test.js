// import { selectSellerReportDomain } from '../selectors';

describe('selectSellerReportDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
