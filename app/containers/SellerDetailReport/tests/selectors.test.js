// import { selectSellerDetailReportDomain } from '../selectors';

describe('selectSellerDetailReportDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
