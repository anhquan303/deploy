// import { selectSellerVoucherDomain } from '../selectors';

describe('selectSellerVoucherDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
