/*
 * DashboardReport Messages
 *
 * This contains all the text for the DashboardReport container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DashboardReport';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DashboardReport container!',
  },
});
